class Solution {
public:
    int maximumWealth(vector<vector<int>>& accounts) {
        int maximum{0};
        for(size_t a{0}; a < accounts.size(); a++){

            for(size_t b{0}; b < accounts.size(); b++){

            }
            if(sum > maximum){
                maximum = sum;
            }
        }
    }
};